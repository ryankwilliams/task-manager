version: '3'

services:
  app:
    env_file:
      - ./docker-compose-vars.env
#    image:
#      quay.io/rywillia/task-manager:latest
    build: .
    restart: always
    ports:
      - "8080:8080"
    depends_on:
      - db
  db:
    image:
      quay.io/sclorg/postgresql-15-c9s:latest
    restart: always
    ports:
      - "5432:5432"
    env_file:
      - ./docker-compose-vars.env
